Description: Add -c option for libtoolize in bootstrap scripts.
Author: Sergey B Kirpichev
Forwarded: yes

---
 bootstrap          |    2 +-
 libmonit/bootstrap |    2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)

--- a/bootstrap
+++ b/bootstrap
@@ -3,7 +3,7 @@
 # autoconf        >= 2.59
 # automake        >= 1.10
 # libtool         >= 1.4
-glibtoolize -f 2>/dev/null || libtoolize -f
+glibtoolize -f 2>/dev/null || libtoolize -f -c
 if aclocal -I config && autoheader && automake --foreign --add-missing --copy && autoconf 
 then
         if cd libmonit && ./bootstrap
--- a/libmonit/bootstrap
+++ b/libmonit/bootstrap
@@ -4,7 +4,7 @@
 # automake        >= 1.10
 # libtool         >= 1.4
 #!/bin/bash
-libtoolize -f 2>/dev/null || glibtoolize -f
+libtoolize -f -c 2>/dev/null || glibtoolize -f
 if aclocal -I config && autoheader && automake --foreign --add-missing --copy && autoconf 
 then
         echo "Success bootstrapping libmonit"
